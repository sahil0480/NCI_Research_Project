name: train-env
dependencies:
  - python=3.10
  - pip
  - pip:
    - pandas
    - scikit-learn
    - xgboost
    - lightgbm
    - matplotlib
    - azureml-core
    - azureml-defaults        # ✅ Optional but recommended for AzureML jobs
    - joblib                  # ✅ Optional if saving/loading models

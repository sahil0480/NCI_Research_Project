apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-media-pvc-rwx
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-rwx
  resources:
    requests:
      storage: 1Gi
